from preprocessing import *
from models import deepcat_cnn

def train(model, criterion, optimizer,
	train_data, train_labels, valid_data, valid_labels,
	nb_epochs=10000, lr=0.001, batch_size):
	
	train_losses = []
	val_losses = []

	for e in range(nb_epochs):
		for b in 
	
	
